# Netlify 部署说明见下方注释；在 Netlify 后台请与此保持一致。
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# 单页应用：任意路径都回到 index.html（避免直接打开子路径 404）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
