# 纯静态站点，不需要 build
[build]
  publish = "."
  command = ""

# 路由规则：/series 或 /series.html → series.html；其余 → index.html
[[redirects]]
  from = "/series"
  to = "/series.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
